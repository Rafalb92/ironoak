{
  "$schema": "https://turborepo.dev/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": ["dist/**", "build/**", ".nuxt/**"]
    },
    "prepare": {
      "dependsOn": ["^build", "^prepare"],
      "outputs": [".nuxt/**"],
      "cache": false
    },
    "lint": {},
    "check-types": {
      "dependsOn": ["^check-types"]
    },
    "test": {
      "dependsOn": ["^build"]
    },
    "dev": {
      "dependsOn": ["^build", "^prepare", "prepare"],
      "cache": false,
      "persistent": true
    }
  }
}
